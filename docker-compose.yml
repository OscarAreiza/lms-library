services:
  # ---- Catalog domain (HU-04, HU-05) ----
  catalog-db:
    image: postgres:16-alpine
    container_name: catalog-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-lms_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-lms_password}
      POSTGRES_DB: catalog_db
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-lms_user} -d catalog_db"]
      interval: 5s
      timeout: 3s
      retries: 5
    volumes:
      - catalog-db-data:/var/lib/postgresql/data
    networks:
      - lms-network

  catalog-migrate:
    image: migrate/migrate:v4.17.1
    container_name: catalog-migrate
    volumes:
      - ./catalog-service/migrations:/migrations
    command:
      - "-path=/migrations"
      - "-database=postgres://${POSTGRES_USER:-lms_user}:${POSTGRES_PASSWORD:-lms_password}@catalog-db:5432/catalog_db?sslmode=disable"
      - "up"
    depends_on:
      catalog-db:
        condition: service_healthy
    networks:
      - lms-network

  catalog-service:
    build:
      context: ./catalog-service
      dockerfile: Dockerfile
    container_name: catalog-service
    environment:
      DB_HOST: catalog-db
      DB_PORT: 5432
      DB_USER: ${POSTGRES_USER:-lms_user}
      DB_PASSWORD: ${POSTGRES_PASSWORD:-lms_password}
      DB_NAME: catalog_db
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be set in .env}
      JWT_EXPIRY: ${JWT_EXPIRY:-1h}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
    depends_on:
      catalog-db:
        condition: service_healthy
      catalog-migrate:
        condition: service_completed_successfully
    networks:
      - lms-network

  # ---- Edge ----
  nginx:
    image: nginx:alpine
    container_name: api-gateway
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "8080:8080"
    depends_on:
      - catalog-service
    networks:
      - lms-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8080/api/v1}
    container_name: lms-frontend
    ports:
      - "3000:80"
    depends_on:
      - nginx
    networks:
      - lms-network

volumes:
  catalog-db-data:

networks:
  lms-network:
    driver: bridge
